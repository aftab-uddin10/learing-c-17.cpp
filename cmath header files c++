#include <iostream>
#include <cmath>
using namespace std ;
int main() {
   int X=5,y=10 ;
   cout << max(X,y) <<"\n";
   cout << min(X,y) <<"\n";
   cout << sqrt(64) << "\n";
cout << round(2.6) << "\n";
cout << log(2);
    return 0;
}
